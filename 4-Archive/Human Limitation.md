

It comes from the human’s ability to:

• compress structure

• retrieve the right pattern

• stay at the right abstraction layer

• resolve ambiguity under time pressure



use the knowledge system and LLM to convert infinite structure into a small number of decision-ready chunks.


OK, but the thing is much of this is a closed feedback system because we’ll use Claude code to do the execution. So I’m just gonna ramble out a little bit, so there is kind of a general pipeline where you have a stakeholder who defines what the entity and state should be, and then you have me and Claude code as the system to define the infinitely queriable dataset that is needed to create a feedback loop that creates that entity. 

  

So really it’s up to me as a designer to understand the system that they are creating, and the transformations that they are requesting and then define they before and after data sets that we then use to create a close feedback loop to infinitely iterate until we have a system that reliably creates that transformation






Your job:

convert requested transformation into a measurable control loop.

  

Claude Code’s job:

execute candidate actions inside that loop.

  

The knowledge system’s job:

store the causal memory so each iteration improves


